{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": { "value": "${AZURE_ENV_NAME}" },
    "location": { "value": "${AZURE_LOCATION}" },
    "deployState": { "value": "${DEPLOY_STATE=false}" },
    "resourcePrefix": { "value": "${RESOURCE_PREFIX=}" },
    "resourceGroupName": { "value": "${AZURE_RESOURCE_GROUP=}" },
    "agentPrincipalId": { "value": "${STATE_AGENT_PRINCIPAL_ID=00000000-0000-0000-0000-000000000000}" },
    "blueprintCredentialMode": { "value": "${W365_BLUEPRINT_CREDENTIAL_MODE=client_secret}" }
  }
}
